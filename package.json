{
  "name": "skystar-ags",
  "version": "1.0.0",
  "description": "ags for skystar",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean" : "rm -rf ./dist",
    "build": "bun build ./config.js --external \"resource://*\" --external \"gi://*\" --external \"file://*\" --outfile dist/main.js",
    "build-css": "bun run ./style/print-style.ts > ./style/varaibles.scss && cd style && dart-sass main.scss ../dist/main.css",
    "run": "npm run build && ags -c ./dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@girs/accountsservice-1.0": "1.0.0-4.0.0-beta.6",
    "typescript": "^5.5.3"
  }
}
